# Postgres connection (local development)
DATABASE_URL="postgresql://neuralflow:neuralflow@localhost:5432/neuralflow?schema=public"
SHADOW_DATABASE_URL="postgresql://neuralflow:neuralflow@localhost:5432/neuralflow?schema=shadow"

# Clerk
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_XXXXXXXXXXXXXXXXXXXX"
CLERK_SECRET_KEY="sk_test_XXXXXXXXXXXXXXXXXXXX"

# Seed (optional – used by prisma/seed.ts to create a Clerk user + tenant)
# Demo user (for sample boards)
# SEED_CLERK_USER_ID="user_demo"
# SEED_CLERK_NAME="Demo User"
# SEED_CLERK_EMAIL="demo@example.com"
# SEED_CLERK_IMAGE="https://.../avatar.png"
#
# Ashwin (your real Clerk account)
# SEED_ASHWIN_USER_ID="user_2abcdXYZ..."  # from Clerk
# SEED_ASHWIN_NAME="Ashwin Rachha"
# SEED_ASHWIN_EMAIL="ashwin@example.com"
# SEED_ASHWIN_IMAGE=null
# SEED_TENANT_SLUG="ashwin"
# SEED_TENANT_NAME="Ashwin Rachha"

# OpenAI
OPENAI_API_KEY="sk-your-openai-key"

# Optional: Prisma Accelerate / Data Proxy (recommended on serverless)
# If set, the app will use this URL instead of DATABASE_URL at runtime.
# Example (Accelerate): prisma://accelerate.prisma-data.net/?api_key=YOUR_KEY
# DATABASE_URL_ACCELERATE="prisma://accelerate.prisma-data.net/?api_key=..."

# Optional: Supabase (fill once you switch)
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_ANON_KEY="your-supabase-anon-key"

# Vercel AI SDK (optional overrides)
AI_MODEL="gpt-4.1-mini"

# Disable dummy embeddings: placeholders are off by default
# Set to 1 only if you want placeholder embedding rows locally while wiring
# the real embedding pipeline.
ENABLE_EMBEDDINGS_PLACEHOLDER="0"

# Scheduler preferences (optional overrides)
# Default work start hour for suggestions
WORK_START_HOUR="9"

# Local dev auth bypass for cURL testing (DO NOT enable in production)
# When set, middleware allows /api/ai and /api/dao without Clerk cookie,
# and get-or-create-user will create a minimal local user record.
DEV_AUTH_BYPASS="0"
DEV_AUTH_BYPASS_USER_ID="user_dev"
